{% extends 'base.html.twig' %}

{% block title %}Echange{% endblock %}

{% block body %}
    <h1>Echange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ echange.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('echange_index') }}">back to list</a>

    <a href="{{ path('echange_edit', {'id': echange.id}) }}">edit</a>

    {{ include('echange/_delete_form.html.twig') }}
{% endblock %}
